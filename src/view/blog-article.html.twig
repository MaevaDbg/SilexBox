{% extends "_layout.html.twig" %}

{% block content %}

    <h1>{{ article.title }}</h1>
    <h2>{{ article.excerpt }}</h2>
    <p>Image : {{ article.image }}</p>
    <p>{{ article.content|raw }}</p>
    <p>{{ article.datePublication|localizeddate("medium", "short", "fr") }}</p>

{% endblock %}