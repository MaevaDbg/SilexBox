{% extends "_layout.html.twig" %}

{% block content %}
    
    {% trans %}titre{% endtrans %}

    {% if articles is empty %}
	   <p>Il n'y a pas encore d'articles mis en avant sur la home</p>
    {% else %}
        <ul>
        {% for art in articles %}
            <li>
                <h1>{{ art.title }}</h1>      
                <p>{{ art.datePublication|localizeddate("medium", "short", "fr") }}</p>
                <p>Image : {{ art.image }}</p>
                <p>{{ art.excerpt }}</p>
            </li>
        {% endfor %}
        </ul>
    {% endif %}

{% endblock %}